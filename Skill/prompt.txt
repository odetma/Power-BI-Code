I have a PBIP workspace with Theme.Report and Theme.SemanticModel.

Task:
Create ONE Power BI theme JSON file only (not separate files), using my guides:
- pbip-theme-extraction
- pbi-style-presets

Requirements:
1) Extract the current report styling from PBIP.
2) Build one combined theme file.
3) Light Mode preset must match the current extracted style exactly.
4) Dark Mode preset must be generated by you.
5) For every visual style/preset family you create, include both:
   - Light Mode
   - Dark Mode
6) Keep style presets structured correctly so they appear in Power BI Style dropdown.
7) Validate final JSON syntax.

Output:
- Save as: theme_with_style_presets.json
- This file must contain both Light and Dark presets in the same theme.

Process rule:
Before making edits, restate your understanding in bullet points and wait for my confirmation.
After completion, summarize exactly which visual sections have both Light and Dark presets.